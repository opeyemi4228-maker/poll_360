# dependencies
/node_modules
/.pnp
.pnp.*

# next
/.next/
/out/
next-env.d.ts

# production
/build

# environment
.env
.env*.local

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# os / editor
.DS_Store
*.pem
.vscode/*
!.vscode/extensions.json
.idea

# test + coverage
/coverage
/test-results

# prisma
/prisma/*.db
/prisma/*.db-journal
/lib/generated

# local database
/data
.env.local

# A scheme is not a path. Guarded in lib/db.js, ignored here as well, because
# the directory this catches carries a live password in its own name.
postgresql:/
