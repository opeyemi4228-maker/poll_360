# Copy to .env.local and adjust.

# The canonical origin, used for metadataBase, sitemap, manifest and JSON-LD.
NEXT_PUBLIC_SITE_URL=https://poll360.ng

# The polling unit agents' own address. When set, the pages in app/agent are
# served at the root of this domain (/login, /join, /pending, /) and the main
# site sends /agent/... across to it. Leave empty to keep them on /agent, which
# is right for local work and preview deployments. To try it locally:
#   NEXT_PUBLIC_AGENT_URL=http://agent.localhost:3000
NEXT_PUBLIC_AGENT_URL=

# Data Bank's agent list. Agents sign in with a code and nothing else, and
# Data Bank confirms it on every sign-in; the approval screen reads the list
# from it too. DATABANK_URL falls back to DUMPSITE_URL. The key is issued in
# Data Bank for POLL360 with the scopes agents:register, agents:approve and
# agents:confirm.
DATABANK_AGENTS_KEY=

# Agent360, for check-ins and the SOS on the agents' app. Both apps hold the
# same AGENT_DOOR_SECRET. Leave both empty and the app has no check-in section.
AGENT360_URL=
AGENT_DOOR_SECRET=

# ---------------------------------------------------------------------------
# Database
#
# Postgres, and it is not optional.
#
# Storage used to be a SQLite file, which is right on one machine and wrong the
# moment this is deployed anywhere serverless: the filesystem does not survive
# between invocations, so every result filed since the last deploy disappears
# without an error. On Vercel it did not even get that far, sign in returned a
# server error because the file could not be opened at all.
#
# Use Neon's POOLED connection string, the host with -pooler in its name. The
# direct host is for migrations and long sessions, and pointing a serverless
# deployment at it exhausts connections under any real load.
#
# Never commit the real value. This file is checked in; .env.local is not.
DATABASE_URL="postgresql://user:password@host-pooler.region.aws.neon.tech/neondb?sslmode=require"
#
# Data Bank's database, added beside the original when that one ran out of
# room. When set, Poll360 reads and saves here instead; DATABASE_URL and the
# original database are left exactly as they are, and unsetting this goes back
# to them. Copy the tables across first with scripts/copy-to-databank.mjs,
# then set this — pointed at Data Bank before the copy, Poll360 finds no tables.
DATABANK_DATABASE_URL=
# ---------------------------------------------------------------------------

# Creating the first account. There is no public sign-up, so the first user is
# made by the seed script and these are read only by it:
#
#   ADMIN_EMAIL=you@poll360.ng ADMIN_PASSWORD='a long one' npm run db:seed
#
# ADMIN_EMAIL=
# ADMIN_PASSWORD=
# ADMIN_NAME=
# ADMIN_PHONE=

# ---------------------------------------------------------------------------
# Encryption at rest.
#
# Seals the fields that would actually hurt if the database were taken:
# incident narratives, agent contact details, result-sheet keys. Vote totals
# stay in the clear, because a column you cannot aggregate is one you cannot draw
# a map from. AES-256-GCM, so a tampered row fails loudly instead of decrypting
# to something else.
#
# Generate one:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#
# Without it the app runs on a known development key and says so on every boot,
# and it refuses to start in production at all.
# 32 random bytes, base64:
#   node -e "console.log(require('node:crypto').randomBytes(32).toString('base64'))"
# This cannot change once data exists. Anything sealed with the old key becomes
# unreadable, permanently, and nothing errors at the point of loss. Missing, the
# app runs on a development key and says so: sealed fields are then NOT secret.
ENCRYPTION_KEY=

# ---------------------------------------------------------------------------
# Still to come with the application tier (see STRUCTURE.md §2):
#
# SHEET_STORAGE_URL=…   where result-sheet bytes live
# SMS_API_KEY=…         one-time codes to field agents
# ---------------------------------------------------------------------------

# ===========================================================================
# DEMONSTRATION LOGINS
#
# Created by `npm run seed:demo`. Four accounts, one per dashboard. They are
# written here because this is an evaluation build. Delete this block and
# change every password before this is anywhere public. Real accounts are
# issued one at a time from the super administrator's dashboard, and their
# passwords are never printed anywhere.
#
#   Super administrator          → /admin
#     email     admin@poll360.ng
#     phone     08030000001
#     password  poll360-super-admin
#
#   Polling unit coordinator     → /field       (booth 25/07/04/019)
#     email     agent@poll360.ng
#     phone     08030000002
#     password  poll360-field-agent
#
#   Broadcast desk               → /broadcast
#     email     broadcast@poll360.ng
#     phone     08030000003
#     password  poll360-broadcast-desk
#
#   Situation room               → /room
#     email     room@poll360.ng
#     phone     08030000004
#     password  poll360-situation-room
#
# Sign in at /login with either the email or the phone number.
# ===========================================================================


#Email     admin@poll360.ng
#Password  election-night-2027
#Login: whatsapp@poll360.ng / poll360-whatsapp-desk. Signs straight into /whatsapp, and is refused /room and /admin. Admin and situation room can also read it.

# ---------------------------------------------------------------------------
# Reading result sheets from photographs
#
# Three readers, and the keys decide which one runs: Anthropic first, then
# Google, then a reader built into the server that needs no key at all.
#
# ONLY THE FIRST CAN READ HANDWRITING, and the figures on an EC8A are written
# by hand. Tested against a real INEC form, the built-in reader recovered no
# vote figure at all — the only number it found on the page was the year in
# the title. If sheets are "not being read", this is almost always why: no
# ANTHROPIC_API_KEY is set, so the built-in reader is doing the work.
#
# Nothing breaks without a key. A reading that cannot be trusted is shown to
# the agent and kept out of the form, and the figures are typed as they always
# were. Set this to get the feature the photograph is actually for.
ANTHROPIC_API_KEY=

# An optical reader, used when there is no Anthropic key. Reads handwriting
# well. Free for the first 1,000 sheets a month, but Google wants a card on
# file before it will issue the key.
GOOGLE_VISION_API_KEY=

# THE ONE YOU CAN TURN ON RIGHT NOW.
#
# OCR.space issues a free key against an email address — no card, no cloud
# account, no billing project — and its handwriting engine reads a real INEC
# form. Measured on one: twelve of fourteen handwritten figures recovered,
# where the built-in reader recovered none.
#
# It is the floor, not the ceiling. Nothing it reads fills an agent's form
# unless the arithmetic on the sheet corroborates it, so its misses show up as
# empty boxes to be typed rather than as wrong figures to be filed.
#
#   Get one at https://ocr.space/ocrapi  (free tier, 25,000 pages a month)
OCRSPACE_API_KEY=

# Override the choice above: claude | google | local | off.
# SHEET_READER=

# ---------------------------------------------------------------------------
# WhatsApp (Meta Cloud API)
#
# All four, or none. Without them the channel still runs end to end: replies
# are recorded as QUEUED and shown on the desk, they simply never leave the
# building. That is what makes it demonstrable without an account.
#
# Where each one comes from, in the Meta developer console:
#
#   WHATSAPP_VERIFY_TOKEN  You invent it. Any long random string. Paste the
#                          same one into Meta's webhook setup box. It is only
#                          used for the one-time subscription handshake.
#   WHATSAPP_APP_SECRET    App settings → Basic → App secret (Show).
#                          Every delivery is signature checked against it.
#   WHATSAPP_TOKEN         WhatsApp → API Setup gives a temporary one good for
#                          24 hours, fine for a first test. For anything
#                          lasting, generate a system user token instead.
#   WHATSAPP_PHONE_ID      WhatsApp → API Setup, "Phone number ID". This is the
#                          long number under the phone number, not the phone
#                          number itself.
#
# Then point Meta's callback URL at:
#   https://your-domain/api/whatsapp/webhook
# and subscribe the WhatsApp account to the "messages" field. Subscribing is a
# separate click from verifying the URL, and nothing arrives until it is done.
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_APP_SECRET=
WHATSAPP_TOKEN=
WHATSAPP_PHONE_ID=

# ---------------------------------------------------------------------------
# Data Bank — where everything this product receives is also delivered
#
# Data Bank is the intake, classification and routing hub behind Poll360 and
# Agent360. Every registration, every photographed result sheet, every figure
# read off one, and everything that arrives over WhatsApp is sent there, where
# it is sealed and chained for the eighteen months between a polling day and a
# tribunal.
#
# Unset, nothing is sent and nothing breaks. The delivery is never on the path
# between an agent and a saved return — a hub having a bad night must not cost
# a polling unit its count — so an unconfigured hub is simply a quiet one.
#
# Whatever does not land is kept in `dumpsite_outbox` and can be sent again:
#
#   npm run databank:replay
#
DATABANK_URL=
DATABANK_API_KEY=

# ---------------------------------------------------------------------------
# The command dashboard's feed
#
# Command is the campaign's own screen: a named candidate, their photograph,
# and their share of a live count. It is the one screen in this product where
# a wrong figure is attributed to a real person by name.
#
# It is therefore off until a real transmission exists. With this unset the
# board is still built and every state is still drawn — there is simply
# nothing in it, and the screen says so above the zeroes rather than leaving a
# reader to wonder whether a quiet night is a broken page.
#
# Set it to `live` when returns are genuinely arriving. Nothing else changes:
# the screen is already reading from the database.
#
#   COMMAND_FEED=live
COMMAND_FEED=

# ---------------------------------------------------------------------------
# Adamawa demonstration accounts
#
# THESE PASSWORDS ARE REAL AND THIS FILE IS COMMITTED. Anyone who can read this
# repository can sign in as any of the four. They are here because they are
# demonstration accounts and being able to hand somebody the file is the point;
# they are not a pattern to follow for a live count, where an account is issued
# from /admin and the credential is read down a phone line once and never
# written anywhere. Rotate any of them by re-running the seeding script, which
# resets rather than duplicating:
#
#   node --env-file=.env.local scripts/seed-adamawa.mjs --commit
#
# Four campaigns in one state, on one project — "Adamawa State, 2023" — each
# pinned to its own contest and its own ground. Signing in as any of them shows
# that ground and nothing outside it: the map, the figures and the coverage
# denominator all narrow together. Sign in at /login.
#
#   Governorship        Adamawa, all 21 local governments
#     governorship@adamawa.poll360.ng
#     quorum-collate-return-witness-80
#
#   Senate              Adamawa Central, 7 local governments
#                       Fufore, Girei, Gombi, Hong, Song, Yola North, Yola South
#     senate.central@adamawa.poll360.ng
#     mandate-notice-witness-declare-34
#
#   Representatives     Yola North/Yola South/Girei, 3 local governments
#     reps.yola@adamawa.poll360.ng
#     sealed-ledger-gazette-district-48
#
#   Local government    Yola North, 1 local government
#     chairman.yolanorth@adamawa.poll360.ng
#     turnout-collate-federal-booth-89
#
# What each one will actually see. The governorship account opens on the real
# declared 2023 result — Fintiri (PDP) 430,861 to Binani (APC) 398,788, against
# a register of 2,196,566. The other three open on their correct ground with no
# declared figures at all, because none were published for those contests in a
# form this repository could reach, and an empty board is the honest answer.
# See lib/adamawa.js for every figure, who holds every seat, and the source for
# each.
